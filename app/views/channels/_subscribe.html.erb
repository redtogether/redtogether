<% if current_user.subscribed?(channel) %>
  <%= button_to "- unsubscribe",
                channel_unsubscribe_path(channel),
                class: "pure-button" %>
<% else %>
  <%= button_to "+ subscribe",
                channel_subscribe_path(channel),
                class: "pure-button" %>
<% end %>